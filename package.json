{
  "private": true,
  "workspaces": [
    "package/*"
  ],
  "scripts": {
    "build": "npm run build -w package/vscode-tree-sitter",
    "clean": "git clean -Xdf"
  },
  "devDependencies": {
		"@types/vscode": "^1.100.0",
    "vite": "^8.1.3"
  }
}