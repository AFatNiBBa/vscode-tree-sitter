
[
  (byte)
  (integer)
  (version)
] @number

(string) @string

(comment) @comment

[
  (ref_class)
  (id_class)
] @class

(id_namespace) @namespace

(part_keyword) @keyword

[
  (part_modifier)
  (type_intrinsic)
] @macro

(id_method) @method

(id_parameter) @parameter

;; (ref_member) @property

(id_label) @label

(part_instruction) @function