{
  "private": true,
  "workspaces": [
    "package/*"
  ],
  "scripts": {
    "build": "npm run build -w package/vscode-tree-sitter && npm run build -w package/vscode-lang-cil",
		"watch": "npm run watch -w package/vscode-lang-cil",
    "vsix": "npm run vsix -w package/vscode-lang-cil",
    "clean": "git clean -Xdf"
  },
  "devDependencies": {
		"@types/vscode": "^1.100.0",
    "vite": "^8.1.3"
  }
}